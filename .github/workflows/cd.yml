# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!

      # Runs a set of commands using the runners shell
      - name: Install OpenConnect
        run: sudo apt-get update && sudo apt-get install -y openconnect

      - name: Connect to VPN
        run: |
          echo "${{ secrets.VPN_PASSWORD }}" | sudo openconnect \
          --protocol=anyconnect \
          -u ${{ secrets.VPN_USERNAME }} \
          --authgroup="Studierende" \
          --passwd-on-stdin \
          ${{ secrets.VPN_ENDPOINT }} \
          --background
          sleep 10
        
      - name: Add SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          
          SSH_ASKPASS_SCRIPT=$(mktemp)
          echo '#!/bin/sh' > $SSH_ASKPASS_SCRIPT
          echo "echo '${{ secrets.SSH_PASSPHRASE }}'" >> $SSH_ASKPASS_SCRIPT
          chmod +x $SSH_ASKPASS_SCRIPT
          DISPLAY=:1 SSH_ASKPASS=$SSH_ASKPASS_SCRIPT setsid ssh-add ~/.ssh/id_rsa

          echo -e "Host 141.72.13.253\n\tStrictHostKeyChecking no\n" >> ~/.ssh/config

      - name: Connect via SSH
        run: ssh pipeline@141.72.13.253 "uptime && whoami"
